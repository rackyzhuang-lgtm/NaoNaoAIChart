# 架构决策记录

本目录保存 Architecture Decision Records（ADR）。已接受的 ADR 不覆写历史；发生变化时新增 ADR 并标记替代关系。

文件命名：`NNNN-short-title.md`。

状态可用：`Proposed`、`Accepted`、`Rejected`、`Superseded`。

每份 ADR 至少包含：背景、决策、备选方案、影响、验证方式、待确认事项。
