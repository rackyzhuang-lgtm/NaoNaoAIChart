# 任务 0052：品牌知识库整理与归档

- 状态：已完成文档整理，等待项目所有者提供下一品牌资料
- 日期：2026-08-10
- 范围：品牌名称、Logo/图标、启动页、安装器、域名与外链、发布身份、Chatbox 遗留内容、应用标识和本地数据迁移边界

## 目标

- 将当前 NaoNaoAI Chat 的品牌事实集中到 `docs/BRAND-INVENTORY.md`。
- 详细记录桌面端、Renderer/Web、移动端和无限画布的 Logo 消费位置。
- 区分用户可见品牌、运行时服务地址、历史兼容标识和第三方归因，阻止无上下文的全仓库替换。
- 为下一次换品牌提供逐项替换顺序、验收矩阵和项目所有者输入清单。

## 本轮完成

- 更新 `docs/BRAND-INVENTORY.md`，新增品牌主数据表、四类 Logo 资产地图、域名/服务边界表、Chatbox 遗留分层、安装/协议/本地键迁移表和验收矩阵。
- 明确记录根包、发布包和发布锁文件中仍保留的上游作者、邮箱、主页和旧包名元数据，避免误以为发布包已完成全量品牌清理。
- 记录 GitHub Release 远程与 Gitee 项目地址的不同职责。
- 未修改业务代码、运行时配置、域名、Logo 资源或应用标识。

## 功能测试与验收标准

- [x] 品牌名称、账户/Agent 子品牌和主要显示位置有唯一记录。
- [x] 桌面、Web、移动端、无限画布 Logo/启动资源及消费位置有映射。
- [x] 官网、模型网关、Canvas allowlist、支付页、代码仓库和发布远程已分层记录。
- [x] Chatbox 内容按用户可见、运行时兼容、技术标识和第三方归因分类。
- [x] appId、包名、深度链接、本地键的迁移风险与兼容要求已记录。
- [x] 下一次替换的输入要求和端到端验收标准已记录。

## 验证记录

- `rg`：已执行，核对 `src/`、`assets/`、`resources/`、构建配置、发布配置、README 和 GitHub Actions 中的品牌名称、域名、Logo 入口及 Chatbox 遗留内容。
- `Get-FileHash`：已执行，确认 `assets/icon.png` 与 `src/renderer/static/icon.png` SHA-256 一致。
- `git diff --check`：通过。
- TypeScript、lint、单元测试、生产构建、打包和桌面 E2E：未执行；本轮仅修改 Markdown 文档。

## 后续工作

收到新品牌资料后，先创建品牌替换任务和必要 ADR，再按 `docs/BRAND-INVENTORY.md` 的执行顺序拆分用户可见层、服务外链层、系统标识层和兼容清理层。
