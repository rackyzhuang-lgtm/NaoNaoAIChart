# 任务 0058：继续同步 EazyAI-Chat v1.22.12 功能

- 状态：已完成
- 日期：2026-08-13
- 源目录：`D:\project\EazyAI-Chat`（只读）
- 源范围：`5b90a211..1fa072d3`，功能提交 `8d8a2920`

## 目标

将源仓库在上次同步之后的可复用功能迁入当前 NaoNaoAI Chat 项目，同时保持当前品牌、服务地址和发布仓库配置。

## 迁入范围

- 无限画布 Agent 工具定义、输入校验、快照压缩和操作请求转换。
- Agent 网关对上述工具的注册、执行和 NaoNaoAI 文案适配。
- 账户页的“我的 / 公告 / 充值 / API 密钥”分区布局及对应本地化文案。
- 侧栏移除旧 Copilots/Help 入口的结构调整。

## 排除项

- 不修改源目录任何文件。
- 排除源仓库未提交的 `LICENSE` 和 `src/__tests__/`。
- 排除源端 EazyAi 品牌、Logo、域名、仓库地址、版本号和发布记录。
- 不迁移源端新增测试文件，除非目标端已有测试需要同步断言；本轮不运行本地构建、打包或完整测试。

## 功能测试与验收标准

- Agent 工具名称、定义和网关路由在目标端可静态检查，写操作仍经画布确认事件。
- 账户页保留 NaoNaoAI 品牌，并显示四个分区入口。
- 侧栏不再包含 `My Copilots`、`Help` 或 `/guide` 入口。
- 固定服务地址和 GitHub 发布远程保持当前目标仓库配置。
- 本轮实际执行：`git diff --check`、`git status --short --branch` 及品牌/敏感路径静态检查。
- 未执行：`pnpm test`、`pnpm build`、桌面 E2E、安装包打包和真实线上请求。
