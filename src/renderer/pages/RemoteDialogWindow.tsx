/**
 * Upstream remote dialogs are disabled so the renderer does not poll Chatbox
 * services or display upstream-owned promotional content.
 */
export default function RemoteDialogWindow() {
  return null
}
